import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.financial.atmosphere',
  appName: 'Financial Atmosphere',
  webDir: 'dist'
};

export default config;
